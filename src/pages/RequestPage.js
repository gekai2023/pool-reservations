import React from "react";
import RequestForm from "../components/RequestForm";
import PageTemplate from "../components/PageTemplate";

const RequestPage = () => {
  return <PageTemplate inner={RequestForm} />;
};

export default RequestPage;
